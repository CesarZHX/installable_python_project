# installable_python_project

Generic installable Python project template.

This repository is intended to be used as a starting point for installable Python projects. Customize the package name, metadata, and source code to match your own project.

## Testing

The project uses **pytest** for running tests. To run the test suite, simply execute:

```bash
pytest
```
